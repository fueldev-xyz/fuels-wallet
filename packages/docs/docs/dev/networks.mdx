---
title: 网络
category: For Developers
---

# 网络

## 添加自定义网络

您可以通过调用 `addNetwork()` 方法向用户的钱包中添加新的网络。

<CodeImport
  file="../../examples/networks/AddNetwork.tsx"
  commentBlock="addNetwork"
/>
<Examples.AddNetwork />

## 获取当前网络

要获取用户当前的网络，可以使用 `currentNetwork()` 方法。

<CodeImport
  file="../../examples/networks/CurrentNetwork.tsx"
  commentBlock="currentNetwork"
/>
<Examples.CurrentNetwork />

## 使用 React

### 添加自定义网络

您可以通过调用 `addNetwork()` 方法向用户的钱包中添加新的网络。

<CodeImport
  file="../../examples/networks/hooks/AddNetworkHook.tsx"
  commentBlock="useAddNetwork"
/>
<Examples.AddNetworkHook />

### 当前网络

您可以使用 `currentNetwork()` 方法跟踪用户当前的网络。

<CodeImport
  file="../../examples/networks/hooks/CurrentNetworkHook.tsx"
  commentBlock="useNetwork"
/>
<Examples.CurrentNetworkHook />
